# Tests: H2 en memoria con modo MySQL; Flyway corre las mismas migraciones que en MySQL.
spring:
  datasource:
    url: jdbc:h2:mem:esquina;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
app:
  mail:
    habilitado: false
  datos-demo: false
